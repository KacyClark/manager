class Task {
    constructor(id, project, priority, deadline, duration, resources, status) {
    this.id = id;
    this.title = project;
    this.deadline = deadline;
    this.duration = duration;
    this.resources = resources;
    this.status = status;

    this.name = "kacy";
    }
}